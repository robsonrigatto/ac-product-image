# Spring boot + JAX-RS + Swagger

## Executar a API
`gradle spring-boot:run` ou `./gradlew spring-boot:run`

## Documentação swagger
> (http://localhost:8080/swagger.json) ou (http://localhost:8080/swagger.yaml)
